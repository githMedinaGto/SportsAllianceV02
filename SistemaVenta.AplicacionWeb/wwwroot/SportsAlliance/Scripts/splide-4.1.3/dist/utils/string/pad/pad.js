/**
 * Pads the number with 0.
 *
 * @param number - A number to pad.
 *
 * @return string - Padded number.
 */
export function pad(number) {
    return number < 10 ? `0${number}` : `${number}`;
}
//# sourceMappingURL=../../../../src/js/utils/string/pad/pad.js.map